import React from 'react';
import NotAuthenticated from '@components/auth/sign-in/NotAuthenticated';

export default {
  title: 'Auth/NotAuthenticated',
  component: NotAuthenticated
};

export const Default = () => <NotAuthenticated />;
